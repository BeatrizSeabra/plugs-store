============
Installation
============

At the command line::

    $ easy_install plugs-store

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv plugs-store
    $ pip install plugs-store
